// Entry point for all unit tests.
// Widget tests are kept minimal because screens depend on live network calls.
// Comprehensive coverage lives in test/models/ and test/services/.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — see test/models/ and test/services/ for real tests', () {
    expect(true, isTrue);
  });
}
